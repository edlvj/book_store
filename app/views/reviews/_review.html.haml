%ul.media-list
  - @book.reviews.approved.each do |review|
    %li.media.card
      .media-left
        %a{href: "#"}
          = image_tag(review.decorate.image, class: "img-circle logo-size media-object")
      .media-body
        %div
          %p.general-message-date.hidden-xs= review.decorate.created_date
          %h4.media-heading.max-240.general-message-name
            %span.general-message-verified Verified Reviewer
            = review.decorate.username
        .mb-15
          - review.rating.times do
            %i.fa.fa-star.rate-star
        %p= review.comment 